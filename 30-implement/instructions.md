# Implement

Do the work described in the card. Tick subtasks in the card as you complete them. When the implementation is done, commit your code changes on this branch, then move the card to the next column with the tool board_move_card.