namespace Yamca.Web.Services;

/// <summary>Where the chat composer/prompt is docked within a chat pane.</summary>
public enum PromptDockPosition
{
    /// <summary>Prompt sits above the message log (default).</summary>
    Top,
    /// <summary>Prompt sits below the message log.</summary>
    Bottom,
}
